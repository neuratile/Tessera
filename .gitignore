# Node / pnpm
node_modules/
.pnpm-store/

# Turbo
.turbo/

# Build outputs
dist/
build/
coverage/
playwright-report/
test-results/

# Env files
.env
.env.local
.env.*.local
.env.*
**/.env
**/.env.*

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Rust (`cargo`) — binaries and intermediates stay out of Git
**/target/

# OS/editor
.DS_Store
Thumbs.db

# Marketing website — separate project, kept out of the app repo
website/

# Local git worktree checkouts — never committed; each is its own checkout
.worktrees/
.claude/worktrees/
