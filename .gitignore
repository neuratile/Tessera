# Node / pnpm
node_modules/
.pnpm-store/
# Lockfile NOTE: package-lock.json was committed alongside the
# pnpm-workspace.yaml scaffold. The workspace uses pnpm; pnpm-lock.yaml
# is the correct lockfile and is intentionally NOT tracked yet to avoid
# shipping two competing lockfiles in the same PR. Follow-up: switch
# to pnpm-lock.yaml and remove package-lock.json (separate PR).
pnpm-lock.yaml

# Turbo
.turbo/

# Build outputs
dist/
build/
coverage/

# Env files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS/editor
.DS_Store
Thumbs.db
